import 'lit-html';
import '@polymer/lit-element';
import { LitElement, customElement } from '@polymer/lit-element';

export function define( name, options ){

  customElement( name )( class extends LitElement{

  });

}